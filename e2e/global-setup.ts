// T2 will replace this with a real signup bootstrap that creates a known tenant + admin
// once per run and saves cookies to e2e/.auth/admin.json.
export default async function globalSetup(): Promise<void> {}
